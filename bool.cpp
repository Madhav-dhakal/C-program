#include<iostream>
using namespace std;
int main(){
	int x,y;
	x=1,y=5;
	if (x>y)
		cout<<"good morning";
		else if (x<y)
		cout<<"good evening";
		else
		cout<<"good day";
		return 0;
}
